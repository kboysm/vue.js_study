yarn add connect-history-api-fallback // 백딴에서의 url로 없는페이지 요청시 프론트 딴의 없는페이지.vue를 뿌려줄 수 있게 해주는 모듈

var history = require('connect-history-api-fallback');
app.user(history()); 해주면 끝

npm install -g win-node-env //최상단 be ,fe를 담고있는 폴더의 package.json 파일의 script 부분 dev,pr을 수행하려는데 명령이 안먹음 , 그걸 해결하는 모듈

yarn global add pm2 // 서버가 잘못된 요청으로 인하여 다운되는것을 방지 ,노드 파일을 돌리는 도구,백그라운드로 돌림 ex) pm2 start 경로 , pm2 stop 이름
->>에러 발견 윈도우10에서 위의 명령으로 할 경우 pm2 -v 로 버전확인이 불가능 -> npm install pm2 -g로 다운로드 해야함

pm2 install pm2-logrotate // 날짜별로 log 데이터를 관리
->pm2 set pm2-logrotate:retain 7 -> 7일동안 관리하라는 의미

yarn add jsonwebtoken // jwt 모듈